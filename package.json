{
  "name": "isoform-workspace",
  "private": true,
  "version": "0.8.3",
  "type": "module",
  "description": "Development workspace for the isoform library and its demo host",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @isoform/demo",
    "catalog": "npm run dev -w @satyadip28/isoform",
    "build": "npm run build -w @satyadip28/isoform && npm run build -w @isoform/demo",
    "build:lib": "npm run build -w @satyadip28/isoform && npm run build:runtime",
    "typecheck": "npm run typecheck -w @satyadip28/isoform && npm run typecheck -w @isoform/demo",
    "test": "vitest run",
    "test:watch": "vitest",
    "pack:check": "npm run build:lib && npm pack -w @satyadip28/isoform --pack-destination .",
    "readme": "node scripts/gen-package-readme.mjs",
    "build:runtime": "node scripts/build-viewer-runtime.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2",
    "vite": "^8.1.5",
    "vitest": "^4.1.10"
  }
}
