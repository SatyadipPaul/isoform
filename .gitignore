node_modules/
dist/
dev-dist/
*.tsbuildinfo
.DS_Store
.vite/

# Local copy of the source catalog, served next to the M1 gate for
# side-by-side comparison. Not ours to commit.
#
# Globbed on the package directory rather than naming it: this rule was pinned
# to `packages/engine/` and stopped matching the moment that directory was
# renamed, which would have published the file.
packages/*/dev/original.html
