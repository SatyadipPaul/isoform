{
  "name": "@isoform/demo",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Demo host for the isoform library \u2014 mounts one editor and nothing else",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "isoform": "*",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.1"
  }
}
